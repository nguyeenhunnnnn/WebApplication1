@model WebApplication1.Models.ViewModels.GoiViewModel

@using WebApplication1.Models

@{
    ViewData["Title"] = "San Pham Dich Vu";
}
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="~/css/styleNav.css" />
    <link rel="stylesheet" href="~/css/StyleSPDV.css" />
    <title>BOWO - Nền tảng Tuyển Dụng</title>

</head>
<body>
    <div class="main-content">
        <section class="hero-section">
            <h1>ĐĂNG TIN TUYỂN DỤNG</h1>
            <p>Cập nhật và hiển thị tin đăng ở vị trí nổi bật</p>
            <a asp-controller="BangTin" asp-action="Index" class="action-btn">Khám phá ngay</a>
        </section>

        <section class="features-grid">
            <div class="feature-card">
                <div class="feature-img">
                    <img src="/api/placeholder/300/200" alt="Calendar and clock illustration">
                </div>
                <h3>Hiển thị ngay lập tức trên trang BOWO</h3>
                <p>Tin tuyển dụng sẽ được hiển thị ngay lập tức tại bowo.vn trong 30 ngày.</p>
            </div>

            <div class="feature-card">
                <div class="feature-img">
                    <img src="/api/placeholder/300/200" alt="Mobile search illustration">
                </div>
                <h3>Linh hoạt và tiết kiệm</h3>
                <p>Nhiều gói dịch vụ đăng tuyển đã đang giúp Nhà tuyển dụng tiết kiệm đến 58% chi phí với quyền lợi sử dụng linh hoạt trong thời hạn 1 năm.</p>
            </div>

            <div class="feature-card">
                <div class="feature-img">
                    <img src="/api/placeholder/300/200" alt="Content management illustration">
                </div>
                <h3>Tùy chỉnh nội dung trong suốt thời gian đăng tuyển</h3>
                <p>Nhà tuyển dụng có thể dễ dàng cập nhật và thay đổi thông tin trong suốt thời gian đăng tuyển.</p>
            </div>

            <div class="feature-card">
                <div class="feature-img">
                    <img src="/api/placeholder/300/200" alt="HR management illustration">
                </div>
                <h3>Thêm lợi ích, không phụ thu</h3>
                <p>Vị trí đăng tuyển sẽ được hiển thị đồng thời tại các trang liên kết thuộc hệ sinh thái BOWO, hoàn toàn miễn phí.</p>
            </div>

            <div class="feature-card">
                <div class="feature-img">
                    <img src="/api/placeholder/300/200" alt="Job description template illustration">
                </div>
                <h3>Thư viện mẫu mô tả công việc gợi ý theo từng chức danh</h3>
                <p>Tham khảo bản mô tả công việc chuyên nghiệp từ thư viện theo từng chức danh đa dạng và luôn cập nhật.</p>
            </div>

            <div class="feature-card">
                <div class="feature-img">
                    <img src="/api/placeholder/300/200" alt="HR Central illustration">
                </div>
                <h3>HR Central - trang quản lý nguồn ứng viên trực tuyến</h3>
                <p>Toàn bộ hồ sơ ứng tuyển nhanh chóng được phân loại, quản lý chuyên nghiệp trong HRCentral giúp tiết kiệm X2 thời gian sàng lọc, theo dõi quá trình ứng tuyển.</p>
            </div>
        </section>

        <section class="pricing-section">
            <h2>SẴN SÀNG XÂY DỰNG ĐỘI NGŨ NHÂN TÀI CHO DOANH NGHIỆP</h2>
            <p class="pricing-disclaimer">*Lưu ý: Giá chưa bao gồm phí VAT</p>

            <div class="pricing-grid">
                @foreach (var goi in Model.GoiDichVus)
                {
                    <form asp-controller="baiDang" asp-action="Index" asp-route-goiId="@goi.Id" asp-route-baiDangId="@Model.BaiDangId" class="goi-form">
                        <div class="pricing-card">
                            <button type="submit" class="goi-button">
                                <h3>@goi.TenGoi</h3>
                            </button>
                            <div class="price">@goi.Gia</div>
                            <div class="pricing-features">
                                @goi.MoTa
                            </div>
                        </div>
                    </form>
                }
        </section>

        <section class="templates-section">
            <h2>MẪU ĐĂNG TUYỂN</h2>
            <div class="templates-grid">
                <div class="template-card">
                    <img src="/api/placeholder/250/350" alt="Job posting template 1" class="template-img">
                </div>
                <div class="template-card">
                    <img src="/api/placeholder/250/350" alt="Job posting template 2" class="template-img">
                </div>
                <div class="template-card">
                    <img src="/api/placeholder/250/350" alt="Job posting template 3" class="template-img">
                </div>
                <div class="template-card">
                    <img src="/api/placeholder/250/350" alt="Job posting template 4" class="template-img">
                </div>
            </div>
        </section>
    </div>
</body>
<footer>
    <div class="footer-grid">
        <div class="footer-section">
            <h4>Về BOWO</h4>
            <ul class="footer-links">
                <li><a href="#">Giới thiệu</a></li>
                <li><a href="#">Tuyển dụng</a></li>
                <li><a href="#">Liên hệ</a></li>
                <li><a href="#">Chính sách bảo mật</a></li>
            </ul>
        </div>

        <div class="footer-section">
            <h4>Dành cho Nhà Tuyển Dụng</h4>
            <ul class="footer-links">
                <li><a href="#">Đăng tin tuyển dụng</a></li>
                <li><a href="#">Tìm hồ sơ ứng viên</a></li>
                <li><a href="#">Báo giá dịch vụ</a></li>
                <li><a href="#">Quảng cáo tuyển dụng</a></li>
            </ul>
        </div>

        <div class="footer-section">
            <h4>Dành cho Ứng Viên</h4>
            <ul class="footer-links">
                <li><a href="#">Tìm việc làm</a></li>
                <li><a href="#">Tạo CV online</a></li>
                <li><a href="#">Cẩm nang nghề nghiệp</a></li>
                <li><a href="#">Tư vấn nghề nghiệp</a></li>
            </ul>
        </div>

        <div class="footer-section">
            <h4>Kết nối</h4>
            <ul class="footer-links">
                <li><a href="#">Facebook</a></li>
                <li><a href="#">LinkedIn</a></li>
                <li><a href="#">YouTube</a></li>
                <li><a href="#">Zalo</a></li>
            </ul>
        </div>
    </div>

    <div class="copyright">
        © 2025 BOWO. Tất cả quyền được bảo lưu.
    </div>
</footer>
</html>
<script>
    // JavaScript for interactive functionality
    document.addEventListener('DOMContentLoaded', function() {
        // Tab switching functionality
        const tabLinks = document.querySelectorAll('.tabs a');

        tabLinks.forEach(function(tab) {
            tab.addEventListener('click', function(e) {
                e.preventDefault();

                // Remove active class from all tabs
                tabLinks.forEach(t => t.classList.remove('active'));

                // Add active class to clicked tab
                this.classList.add('active');

                // Here you would typically update content based on tab
                // For example: showContent(this.getAttribute('data-tab'));
            });
        });

        // Pricing card hover effects
        const pricingCards = document.querySelectorAll('.pricing-card');

        pricingCards.forEach(function(card) {
            card.addEventListener('mouseenter', function() {
                this.classList.add('hover');
            });

            card.addEventListener('mouseleave', function() {
                this.classList.remove('hover');
            });
        });

        // Mobile menu toggle (if needed)
        const menuToggle = document.createElement('button');
        menuToggle.className = 'menu-toggle';
        menuToggle.innerHTML = '<span></span><span></span><span></span>';
        document.querySelector('.header-container').appendChild(menuToggle);

        menuToggle.addEventListener('click', function() {
            document.querySelector('.main-nav').classList.toggle('active');
        });

        // Smooth scroll to sections
        const ctaButton = document.querySelector('.cta-button');

        ctaButton.addEventListener('click', function(e) {
            e.preventDefault();
            document.querySelector('.pricing').scrollIntoView({
                behavior: 'smooth'
            });
        });

        // Template gallery functionality
        const templates = document.querySelectorAll('.template');

        templates.forEach(function(temp, index) {
            temp.addEventListener('click', function() {
                // Show template preview or details
                alert('Đang xem chi tiết mẫu đăng tuyển ' + (index + 1));
                // In a real implementation, you might open a modal with template details
            });
        });
    });

    // Function to handle form submission for job posting
    function submitJobPosting(formData) {
        // This would be implemented to send data to server
        console.log('Dữ liệu đăng tin:', formData);
        // Example AJAX call that would be implemented
        /*
        fetch('https://api.bowo.vn/job-posting', {
            method: 'POST',
            body: JSON.stringify(formData),
            headers: {
                'Content-Type': 'application/json'
            }
        })
        .then(response => response.json())
        .then(data => {
            if(data.success) {
                alert('Đăng tin thành công!');
            } else {
                alert('Có lỗi xảy ra: ' + data.message);
            }
        })
        .catch(error => {
            console.error('Error:', error);
            alert('Có lỗi khi gửi dữ liệu');
        });
        */
    }

    // Function to handle package selection
    function selectPackage(packageId) {
        const packages = {
            'regular': {
                name: 'REGULAR POSTING',
                price: '1.750M',
                features: ['Đăng tin trong 30 ngày', 'Hiển thị đồng thời trên mobile app & trang web']
            },
            'extra': {
                name: 'EXTRA PACKAGE',
                price: '2.880M',
                features: ['Đăng tin trong 30 ngày', 'Tiêu đề in Đậm trong 30 ngày']
            },
            // Add other packages here
        };

        const selectedPackage = packages[packageId];
        if(selectedPackage) {
            console.log('Đã chọn gói:', selectedPackage.name);
            // In a real implementation, you would update UI or redirect to checkout
            return selectedPackage;
        }

        return null;
    }

    // Function to toggle dark/light mode (could be implemented)
    function toggleTheme() {
        const body = document.body;
        body.classList.toggle('dark-mode');

        // Save preference to localStorage
        const isDarkMode = body.classList.contains('dark-mode');
        localStorage.setItem('darkMode', isDarkMode);

        // Update theme stylesheet
        const themeStyle = document.getElementById('theme-style');
        themeStyle.href = isDarkMode ? 'dark-styles.css' : 'styles.css';
    }

    // Function to handle search functionality
    function searchJobs(query) {
        console.log('Tìm kiếm:', query);
        // In a real implementation, this would fetch results from API
        // and update the page content
    }
</script>


